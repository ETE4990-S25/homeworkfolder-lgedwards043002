import time
import math

def is_prime(n):
    if n <= 3:
        return n > 1
    if n % 2 == 0 or n % 3 == 0:
        return False
    i = 5
    w = 2
    while i * i <= n:
        if n % i == 0:
            return False
        
        i += w
        w = 6 -w
    
    return True

if __name__ == "__main__":
    start = time.time()
    count = 0
    for i in range(10,000):
        if is_prime(i):
            count += 1
    print(f"Found {count} primes in {time.time() - start:.4f} sec")
